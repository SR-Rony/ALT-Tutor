# Public — safe for browser
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api

# Set to "false" to call the NestJS backend (phone + JWT auth)
NEXT_PUBLIC_USE_MOCK_API=false
